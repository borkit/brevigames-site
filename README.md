# brevigames.com

The published website. Right now it is a holding page.

`public/` is what gets served, and it is the whole of it: static HTML and one stylesheet, no build
step, no JavaScript, no analytics and no trackers. Pushing to `main` publishes it.
